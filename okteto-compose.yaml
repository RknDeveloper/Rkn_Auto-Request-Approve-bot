version: "3.10"
services:
  worker:
    build: .
    environment:
      - API_ID=${API_ID}
      - API_HASH=${API_HASH}
      - BOT_TOKEN=${BOT_TOKEN}
      - UPDATECHANNEL_ID=${UPDATECHANNEL_ID}
      - ADMIN=${ADMIN}
      - UPDATE_CHANNEL=${UPDATE_CHANNEL}
      - MONGO_URI=${MONGO_URI}
